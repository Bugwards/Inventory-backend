server:
  port: 8081

spring:
  application:
    name: stock-issue-service

  datasource:
    url: jdbc:postgresql://localhost:5432/test_merged
    username: postgres
    password: Psa@1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    user:
      name: user
      password: user123

cors:
  allowed-origins: http://localhost:3000